{
  "name": "cloud_api",
  "module": "index.ts",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "bun run src/index.ts --watch ./src/**/*.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "bun build ./src/**/*.ts --outdir ./dist --target bun --sourcemap --minify",
    "start": "bun run src/index.ts",
    "lint": "eslint ./src --ext .js,ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/express": "^5.0.0"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "express": "^4.21.1",
    "firebase-admin": "^13.0.1"
  }
}